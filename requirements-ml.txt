# Extras for deep-learning models (V1 / V2 / V3)
-r requirements.txt

torch>=1.9
torchvision>=0.10
catboost>=1.0
scipy>=1.5

# CPU-accelerated inference — pick ONE:
#   onnxruntime            -- plain CPU EP (MKL); 3x over raw PyTorch
#   onnxruntime-openvino   -- adds OpenVINO EP (Intel CPUs only); another 1.5-2x
# Uncomment whichever applies:
# onnxruntime>=1.15
# onnxruntime-openvino>=1.15

# --- V3: stroke classification (tennisvision/action/) ---
# YOLOv8 person tracking + MoveNet TFLite pose + Keras GRU stroke classifier
# (forehand/backhand/serve). Uncomment if you set action.enabled=true.
# tensorflow>=2.10        # covers both the .h5 Keras model AND the TFLite interpreter
# ultralytics>=8.0        # YOLOv8 person detection + ByteTrack tracking
# tflite-runtime>=2.10    # lighter alternative IF you only need MoveNet (not the .h5 RNN)
